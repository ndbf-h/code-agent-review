# 安全策略

## 支持的版本

| 版本 | 支持状态 |
| --- | --- |
| `master` / `develop` 最新提交 | 接收安全修复 |
| 更早的历史提交 | 不接收 |

## 报告安全问题

请勿通过公开 Issue 披露安全问题。请使用以下任一渠道私下联系：

- GitHub 私密安全公告：仓库 Security 标签页 → Advisories → Report a vulnerability
- 或在 Issue 中仅说明「存在安全问题，需要私下沟通渠道」，等待维护者联系

报告中请包含：受影响的提交或版本、复现步骤、影响范围、概念验证（如有）、你的联系方式。

## 响应时限

| 阶段 | 目标 |
| --- | --- |
| 确认收到 | 3 个工作日内 |
| 初步评估与分级 | 7 个工作日内 |
| 修复或缓解方案 | 按严重程度，14 个工作日内给出计划 |
| 公开披露 | 修复发布后，与报告者协商时间 |

## 已实现的安全控制

- **入参校验**：全部 HTTP 入参经 zod 校验，`:id` 强制 UUID，非法输入返回 400 与字段级错误（REQ-04）
- **HTTP 加固**：helmet 安全响应头、CORS 白名单、请求体上限（超限 413、非法 JSON 400）、`trust proxy` 可配（REQ-06）
- **鉴权**：可选 API Key（`API_KEYS`），支持 `X-API-Key` 与 `Authorization: Bearer`；SSE 路径支持 `?api_key=`（EventSource 无法自定义请求头）；密钥比较为恒定时间算法（REQ-12）
- **限流**：全局限流 + 任务接口限流，`/mcp` 同样受限（REQ-06）
- **配置安全**：`DATABASE_URL`、`RABBITMQ_URL` 必填，无内置明文口令回退，缺失即启动失败并列出全部错误字段（REQ-03）
- **日志**：结构化日志不记录请求体；5xx 响应附带 `requestId` 便于定位（REQ-05）
- **LLM 输入隔离**：被审查代码、文件内容与抓取结果统一经带随机 nonce 的定界标签包裹，角色 prompt 内置防注入约束，并做启发式注入检测（REQ-10）
- **输出脱敏**：报告写入前对问题描述与建议执行密钥掩码；规则引擎命中片段不保留密钥原文（REQ-10）
- **成本治理**：单任务 token 预算、ReAct 工具调用上限、相同调用熔断（REQ-11）
- **容器**：server 与 client 镜像均以非 root 运行，前端静态资源由 nginx 托管（REQ-08）
- **Webhook**：GitHub Webhook 使用 HMAC SHA-256 校验签名，恒定时间比较；未配置密钥时端点返回 404（REQ-15）

## 已知边界与限制

1. **SSRF 防护属于应用层**：URL 抓取限制协议、重定向次数与响应体积，并在 DNS 解析后校验私有、回环、链路本地、保留与组播地址。这不能替代网络出口策略——公网部署建议叠加防火墙、容器网络策略或出网代理，以降低 DNS Rebinding 等风险。
2. **限流为单实例内存态**：多副本部署需迁移到集中式存储（如 Redis）。
3. **指标为进程内计数器**：Token 用量与 Agent 耗时按进程统计，多副本场景需跨实例聚合。
4. **API Key 是共享密钥**：没有用户体系与权限隔离，密钥泄露后需轮换 `API_KEYS` 并重启。
5. **Prompt injection 只能缓解**：检测规则是启发式的，定界与角色约束能显著降低风险，但无法保证 100% 阻断；命中时前端会给出提示，报告结论仍建议人工复核。
6. **审查内容会发送给所配置的 LLM 服务商**：请勿提交含真实密钥或敏感数据的代码。系统会对输出脱敏，但输入本身会离开本机。
7. **`/mcp` 端点无独立权限模型**：与 `/api` 共用 API Key，若暴露到公网请在代理层限制来源。

## 生产部署清单

- 设置 `NODE_ENV=production`、明确的 `CORS_ORIGINS`、非空 `API_KEYS`
- 配置 `TRUST_PROXY` 与反向代理层数匹配，否则按 IP 的限流会失效
- 为 PostgreSQL、RabbitMQ 使用强口令并限制网络可达范围
- 通过 HTTPS 暴露，并在代理层限制 `/mcp` 的外部访问
- 仅在使用 Webhook 时配置 `GITHUB_WEBHOOK_SECRET`，未使用时保持为空即自动关闭该端点
